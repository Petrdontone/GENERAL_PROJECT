name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run: |
         git clone https://github.com/Petrdontone/GENERAL_PROJECT
         cd GENERAL_PROJECT
         pwd
         ls -la
         echo Hello, world!
         docker build .
         pwd
         pip install -r requirements.txt 
         mkdir folder && cd folder && pwd

      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.

  jira_script:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Run a one-line script
        run: |
         echo Hello, world!
         pwd && cd ..
         pwd
         ls -la
         pip install jira
         python -m jira_helper
         mkdir folder && cd folder1

      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.          
